{% set classes = classes|default([])|merge([
  'site-header'
]) %}

<header {{ attributes }}>
	<div class="{% for class in classes %} {{class}}{% endfor %}">
		<div class="container">
			{% block region_content %}
				{% if region_content %}
					{{region_content}}
				{% else %}
					<div>Region content here.</div>
				{% endif %}

			{% endblock %}
		</div>
	</div>
</header>
